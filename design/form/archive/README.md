# Superseded — do not read these as instructions

**These are the record of what was sent, not the current brief.** The current brief is
`design/form/BRIEF-lineup-v2.md`, one directory up, and it supersedes everything here.

They are archived rather than deleted because the reproducible unit is
`(skill + seed + prompt)` and a run cannot be reproduced without the prompt that produced
it. But a design session seeded from this repo will read whatever it finds, and these
contradict the current brief in three places — so they live behind this notice.

| File | Was | Superseded because |
|---|---|---|
| `PROMPT.md` | pass 01 — First Chair form | Gave a parts list with **no mechanism**, so the slider was invented as Daily Driver's thumbscrew friction lock. Described the house style in adjectives instead of pointing at the archive. |
| `PROMPT-02-family-yoke.md` | pass 02 — the yoke | Still correct in substance, but written against a 13 mm wire bow that does not exist and a slider being deleted. |
| `PROMPT-03-upper-assembly.md` | pass 03 — bow, slider, attachment | Absorbed verbatim into §6 and §7 of the current brief. |

Three corrections that should have been one brief. The pattern is recorded in
`docs/DESIGN-LOG.md`.
