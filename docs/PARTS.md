# First Chair — parts inventory

**Generated by `build.py` — do not hand-edit; add/remove parts in `build.py`.**

**17 parts total** — 4 printed · 7 accessory · 2 fit coupon · 4 reference. A full `build.py` / `gate.py` run reports **17/17**. Sourcing + quantities are in `BOM.md`.

## Printed

| Part | Output | Role |
|---|---|---|
| `cup` | STL + STEP | earcup shell — integral lattice grille, baffle bosses, pivot bosses, pad lip |
| `baffle` | STL + STEP | front driver plate — aperture, guard, controlled vents, driver seat + collar |
| `yoke` | STL + STEP | fork — tilts on the cup pivot, carries the height-adjust post |
| `slider` | STL + STEP | headband clamp + post barrel — height lock (knob → shoe → post) + swivel |

## Accessory — printed, not in the reference assembly

| Part | Output | Role |
|---|---|---|
| `driver_clamp` | STL + STEP | 3-ear ring retaining the driver against the baffle back |
| `adapter_ring` | STL + STEP | step-down ring to host a smaller driver in a bigger baffle (optional) |
| `headband_pad` | STL + STEP | crown cushion under the bow (TPU) |
| `grille_dot` | STL + STEP | press-in accent cap at the grille centre (the mark's orange dot) |
| `headband_clamp` | STL + STEP | inner cover plate that sandwiches the metal bow end |
| `slider_shoe` | STL + STEP | captive pressure shoe — knob presses it onto the post (no marring) |
| `vent_plug` | STL + STEP | press-fit plug for the closed-back tuning ports (reversible openness knob) |

## Fit coupon — printed QA tool, not in the assembly

| Part | Output | Role |
|---|---|---|
| `driver_coupon` | STL + STEP | FIT COUPON — driver seat/collar + clamp standoff interface (QA) |
| `pad_coupon` | STL + STEP | FIT COUPON — pad-skirt grip on the cup OD (QA) |

## Reference — not printed (bought part / mockup)

| Part | Output | Role |
|---|---|---|
| `bow` | STEP only | bought Beyer metal head bow (917017/973361) or DIY spring-steel blank (reference) |
| `yoke_rod` | STEP only | the adjustment-post shoulder screw, ISO 7379 Ø6×M5×50 (reference viz) |
| `driver` | STEP only | representative 40 mm driver mockup (reference) |
| `earpad` | STEP only | representative round earpad mockup (reference) |
